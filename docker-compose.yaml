version: '3'

services:
  api:
    build: ./back/
    command: sh -c "PYTHONPATH=/app/back uvicorn main:app --reload --port=8000 --host=0.0.0.0"
    ports:
      - 8000:8000
    volumes:
      - .:/app